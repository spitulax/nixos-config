--- Saves session state so neovim will get back to the state of previous session after it has been closed.

---@type PluginConfig
return {
  spec = {
    "rmagatti/auto-session",
    lazy = false,
  },
}
